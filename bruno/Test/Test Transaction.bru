meta {
  name: Test Transaction
  type: http
  seq: 3
}

post {
  url: {{base_url}}/transactions/{{order_id}}/complete
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
