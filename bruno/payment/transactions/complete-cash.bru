meta {
  name: Complete Cash Transaction
  type: http
  seq: 9
}

http {
  method: POST
  url: {{base_url}}/transactions/{{order_id}}/complete
  auth: bearer
  body: none
}

auth:bearer {
  token: {{access_token}}
}
