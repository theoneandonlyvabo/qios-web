meta {
  name: Update Product
  type: http
  seq: 3
}

patch {
  url: {{base_url}}/products/{{product_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{owner_token}}
}

body:json {
  {
    "name": "Nasi Goreng Spesial XL",
    "price": 30000,
    "is_available": true
  }
}
