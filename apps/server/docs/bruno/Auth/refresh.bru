meta {
  name: Refresh Token
  type: http
  seq: 4
}

post {
  url: {{base_url}}/auth/refresh
  body: none
  auth: bearer
}

auth:bearer {
  token: {{owner_token}}
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("owner_token", res.body.data.access_token);
  }
}
