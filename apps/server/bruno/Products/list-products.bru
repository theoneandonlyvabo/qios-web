meta {
  name: List Products
  type: http
  seq: 1
}

get {
  url: {{base_url}}/products
  body: none
  auth: bearer
}

auth:bearer {
  token: {{owner_token}}
}

params:query {
  category:
  q:
  sort: popular
}
